# Backend Configuration
REACT_APP_BACKEND_URL=http://localhost:3001

# Firebase Configuration (Replace with your actual Firebase project credentials)
REACT_APP_FIREBASE_API_KEY=your_firebase_api_key_here
REACT_APP_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your_project_id_here
REACT_APP_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your_sender_id_here
REACT_APP_FIREBASE_APP_ID=your_app_id_here
REACT_APP_FIREBASE_MEASUREMENT_ID=your_measurement_id_here

# Google Generative Language API (Gemini) - WARNING: do not commit real API keys.
# If you need to call the Google Generative Language API from client-side code,
# store your API key in a local `.env` file and never commit it to version control.
# For production, prefer a server-side proxy that keeps the key secret.
REACT_APP_GOOGLE_API_KEY=your_google_api_key_here

# -----------------------------------------------------------------------------
# Testing examples (do NOT commit real keys)
# This section shows quick commands you can run locally to test the Generative
# Language API using the `REACT_APP_GOOGLE_API_KEY` value. These are examples
# only — set the real key in a local `.env` file or in your shell session.
# -----------------------------------------------------------------------------

# Example: curl (Windows curl.exe / git-bash / WSL)
# Replace YOUR_KEY or set the environment value in your current shell.
# curl.exe -X POST \
#   -H "Content-Type: application/json" \
#   -d '{"contents":[{"parts":[{"text":"Explain how AI works in a few words"}]}]}' \
#   "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=$REACT_APP_GOOGLE_API_KEY"

# Example: PowerShell (preferred on Windows)
# # set the key for this session (or store in frontend/.env)
# $env:REACT_APP_GOOGLE_API_KEY = 'YOUR_KEY'
# $body = @{
#   contents = @(
#     @{ parts = @(@{ text = 'Explain how AI works in a few words' }) }
#   )
# } | ConvertTo-Json -Depth 10
# Invoke-RestMethod -Uri "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=$env:REACT_APP_GOOGLE_API_KEY" \
#   -Method Post -ContentType 'application/json' -Body $body

# Reminder: For production, do NOT expose the API key to client-side code. Use
# a server-side proxy that keeps the key in server environment variables.